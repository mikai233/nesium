/// Netplay constants shared across UI/widgets.
///
/// Keep these in one place to avoid magic numbers sprinkled throughout the app.

/// Player index value representing a spectator (matches the wire protocol).
const int spectatorPlayerIndex = 0xFF;
